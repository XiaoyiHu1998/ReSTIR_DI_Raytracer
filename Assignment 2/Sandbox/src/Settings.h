#pragma once

namespace Settings
{
	// Add settings to be used globally in program and ImGui

	// Rendering Resolution (independent from imgui viewport size)
	// uint32_t Rendering Kernel size
	// uint32_t Samples per pixel
	// enum Render paths

}